//! config/passport/strategies/discord.js

/**
 * Discord OAuth strategy scaffold
 *
 * Responsibilities:
 * - Register Passport OAuth2 strategy for Discord
 *
 * Why this file exists:
 * - Keeps Discord auth configuration isolated
 * - Prepares future account linking/sign in flow
 *
 * Notes:
 * - Generic OAuth2 will be used for Discord
 * - Environment variables and DB logic will be added later
 */

/**
 * Register Discord OAuth strategy
 *
 * @param {import('passport').PassportStatic} _passport
 */
function setupDiscordStrategy(_passport) {
	// Strategy setup will be added later.
}

export default setupDiscordStrategy;
