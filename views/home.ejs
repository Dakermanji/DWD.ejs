<!--! views/home.ejs -->

<h1><%= t('home.title') %></h1>
<p><%= t('home.description') %></p>

<% languages.forEach(lang => { %>
	<a href="/language/<%= lang.code %>">
		<img src="/images/flags/<%= lang.flag %>.svg" width="36" height="27">
		<%= lang.name %>
	</a>
<% }) %>
